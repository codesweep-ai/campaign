{
  "id": "SAC-024",
  "title": "A mission can name outcome values the product does not have, and nothing says so",
  "type": "defect",
  "severity": "med",
  "status": "closed",
  "foundBy": "session agent, from a fresh-agent test: the mission it wrote declared campaign-partial and campaign-unmet, and briefed the orchestrator to reply with one of them",
  "opened": "2026-09-09",
  "resolved": "2026-09-09",
  "stint": "2026-09-09 scaffold prompts session",
  "evidence": {
    "commits": [
      "a652867"
    ],
    "integrated": [],
    "verified": "go test ./internal/cli/ -run TestAnInventedOutcomeIsNamedBeforeTheSpend -count=1: PASS. Proved by mutation: widening the pattern to bare text reports campaign-free as an invented outcome, and the test fails on it. End to end, the mission the test agent wrote now draws four warnings by file and token, and a mission naming only real values is silent. make check exit 0."
  },
  "resolution": "`validate`, `plan` and `create` warn when a seeded document names an outcome the product does not have, before anything is provisioned. The warning names the file, the invented token and the four real values.\n\nThe pattern matches a value where an author is naming it as a value, inside backticks or after `--outcome`. Bare text is left alone on purpose: this document set writes campaign-free and campaign-specific in ordinary prose, and warning on those would make the warning worthless.\n\nA warning rather than a refusal, because a document may name an outcome for a reason this cannot see. Checking a closed vocabulary the product owns is not grading prose, which R50 forbids.",
  "details": "A seeded document can declare an outcome vocabulary the product will reject, and every pre-spend command accepts it.\n\nAn agent given only the binary wrote a mission naming `campaign-met`, `campaign-partial` and `campaign-unmet`. Two of those do not exist. The values are `campaign-met`, `campaign-converged`, `campaign-exhausted` and `campaign-blocked`. It then briefed the orchestrator to close the campaign with `campaign-partial`, which the reply verb refuses.\n\nThe failure lands late and expensively. The mission is seeded at create, the orchestrator works the whole campaign against a vocabulary it cannot use, and the refusal arrives when it tries to report. Nothing before that reads the words.\n\nThis is not a documentation gap. MANUAL states the four values in the same sentence as the `--outcome` flag, and the agent cited that sentence. It had the vocabulary and wrote different words anyway.\n\nChecking it is not grading content, which R50 forbids. The outcome vocabulary is a closed set the product owns, in the way the `--set` override paths are a closed allowlist. A seeded document that names a value outside the set is wrong however good the prose around it is.\n\nThe same shape was fixed once already in the scaffolded mission, which named `Met` and `Converged`. Removing the stub took away that instance and left the class in place.",
  "notes": [
    {
      "date": "2026-09-09",
      "text": "The same commit rewrote part of the playbook, for a related reason. It presented a brief's parts as headings, which read as a schema that nothing enforces. They are now the three questions the readback asks, which are the part that is real. Inventing a heading breaks nothing, and leaving out what the member has to answer does. The orchestrator's brief is also told not to restate the outcome vocabulary at all."
    }
  ],
  "links": [
    "SAC-021"
  ]
}
