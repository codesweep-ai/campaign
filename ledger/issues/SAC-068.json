{
  "id": "SAC-068",
  "title": "A copied agent login expires with the token it was copied from, and neither the warning nor the repair says so",
  "type": "defect",
  "severity": "med",
  "status": "closed",
  "foundBy": "user report (host), from the operator postmortem of a campaign whose orchestrator held a copied claude login and failed about eight hours after create",
  "opened": "2026-09-25",
  "resolved": "2026-09-26",
  "stint": "2026-09-26 session",
  "evidence": {
    "commits": [
      "4c0176c"
    ],
    "integrated": [],
    "verified": "`go test ./internal/cli/ -count=1`: `TestACopiedLoginIsSaidToExpireWithItsToken` PASS. It shows a seat granted `inheritAgentLogin` gets the lifetime line and a seat whose key displaces its login does not. `TestTheDocumentedCopiedLoginWarningIsTheOnePrinted` PASS. It holds MANUAL.md's quote to the printed line, and MANUAL.md and PLAYBOOK.md to the split repair. `make check` exit 0, and `make ci` exit 0 with all 9 smoke scenarios replayed."
  },
  "resolution": "The record's first two remedies landed as its documentation half. `validate`, `plan` and `create` print a second warning for a seat holding a copied agent login. It says the copy does not renew, lasts as long as the access token it was copied from, and is not reached by a host sign-in. MANUAL.md and PLAYBOOK.md now split the repair. A lent credential is renewed on the host and the session restarted. A held one needs a sign-in inside the member, or the seat is recreated. The warning does not set the token's expiry against the campaign's deadline, which the first remedy also asked for. That comparison is not done. Moving the orchestrator to a lent login is not done here. It waits on sandbox records SBX-030 and SBX-064, and the decision is Rishi's.",
  "details": "**What happened.** A campaign's orchestrator was granted `inheritAgentLogin: [claude]`, so its member held a copy of the host's login. It kept working through the night. About eight hours after the host sign-in that preceded `create`, its next turn failed with the CLI's `remote Claude login expired`. The copy had not renewed itself, and a later sign-in on the host did not reach it. `cs-campaign restart <campaign>` starts a new session on the same expired copy, so it cannot repair this. The repair was to sign in again inside the member.\n\n**What the tool says.** `validate`, `plan` and `create` warn that a seat holding a copy keeps it \"until it is destroyed, and the host cannot revoke it\". That is true, and it is about revocation. Nothing says that the copy lives only as long as the access token that was copied, about eight hours for claude, or that it does not renew. MANUAL.md says of logins only that \"A lent one is read fresh on every call, so signing in again on the host is all a stale one needs.\" For a rejected credential it says \"The repair is to renew the key, and then to restart the member's session with `cs-campaign restart`\". That repairs a lent seat and not a held one. PLAYBOOK.md says only \"A rejected credential is yours to renew.\"\n\n**Why it matters.** A campaign whose deadline outlasts the copied token fails at a time nobody chose: whenever the operator last signed in on the host before `create`. The documented repair then does not work, and the operator has to find the one that does while the campaign waits.\n\n**How to reproduce.** Sign in on the host, then create a campaign whose orchestrator is granted `inheritAgentLogin: [claude]`, with a deadline more than eight hours away. Sign in on the host again part way through. The orchestrator's first turn after the original token expires fails with `remote Claude login expired`, and `cs-campaign restart <campaign>` does not bring it back.\n\n**What would help.** Any of these would close the gap:\n\n1. `validate` and `plan` state the held login's lifetime beside the existing warning, and the time it expires set against the campaign's deadline. They warn when the deadline is later.\n2. The manual and the playbook name the repair for a held login: sign in inside the member, because a sign-in on the host does not reach the copy.\n3. `restart` copies the host's current login in again, for a seat whose grant inherits, before it starts the new session.\n\nLending the login avoids the whole problem, because a lent seat reads the host's login on every call.",
  "notes": [],
  "links": []
}
